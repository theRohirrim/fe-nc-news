const ArticleCard = (props) => {
    const {title, author, topic, created_at, votes, article_img_url, comment_count} = props
    return (
        <div id="article-card">
            <h3>ARTICLE CARD</h3>
        </div>
    )
}

export default ArticleCard